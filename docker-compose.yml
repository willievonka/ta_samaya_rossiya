services:
  rossiya-webapi:
    image: webapi
    build:
      context: .
      dockerfile: WebAPI/Dockerfile
    ports:
      - "8080:80"
    environment:
      - ASPNETCORE_URLS=http://+:80
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__MapDBConnection=Server=rossiya-postgres;Database=ta-samaya-rossiya;User Id=postgres;Password=2018;
    depends_on:
      - "rossiya-postgres"
    networks:
      - aspnetcoreapp-network

  rossiya-postgres:
    image: postgis/postgis:16-3.4
    restart: always
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 2018
      POSTGRES_DB: ta-samaya-rossiya
    networks:
      - aspnetcoreapp-network
    volumes:
      - dbdata:/var/lib/postgresql/data

networks:
  aspnetcoreapp-network:
    driver: bridge

volumes:
  dbdata: